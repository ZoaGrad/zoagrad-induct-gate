{
  "name": "sovereign-induction-gate",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:web": "npm run dev --workspace=web",
    "dev:contracts": "npm run dev --workspace=contracts",
    "dev:bot": "python3 bot/main.py",
    "supabase:push": "echo \"'supabase db push' must be run from your local machine with the Supabase CLI installed.\"",
    "docker:up": "docker-compose -f ops/docker-compose.yml up --build",
    "docker:down": "docker-compose -f ops/docker-compose.yml down"
  },
  "workspaces": [
    "contracts",
    "web"
  ]
}
